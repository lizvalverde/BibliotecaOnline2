@using WebBibliotecaOnline2.Models

@model List<TipoBiblioteca>
@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>
<div>
    <table border="1">
        <thead>
            <tr>
                <th>
                    Tipo
                </th>
                <th>
                    Descripcion
                </th>
            </tr>
        </thead>
        <tbody>
            @foreach (var item in Model)
            {
                <tr>
                    <td>
                        @item.TipoBibliotecaId
                    </td>

                    <td>
                        @item.Nombre
                    </td>
                    <td>
                        <a href="@Url.Action("Eliminar","TipoBiblioteca",new { id= item.TipoBibliotecaId })">Eliminar</a>
                    </td>
                </tr>

            }
        </tbody>
    </table>
</div>

